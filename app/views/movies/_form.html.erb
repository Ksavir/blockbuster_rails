<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class:"text-light d-block" %>
    <%= form.text_field :name, class: "mt-1" %>
  </div>

  <div>
    <%= form.label :client_id, class:"text-light d-block mt-2" %>
    <%= form.select :client_id, options_for_select(@clients), class: "mt-1" %>
  </div>

  <div class="mt-3">
    <%= form.submit class:"btn btn-outline-warning btn-sm" %>
  </div>

<% end %>
