<%= form_with(model: client) do |form| %>

  <!-- Error handler -->
  <% if client.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
        <% client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Edit handler -->

  <div class="">
    <%= form.label :name, class:"text-light d-block" %>
    <%= form.text_field :name, class: "mt-1" %>
  </div>

  <div>
    <%= form.label :age, class:"text-light d-block mt-2"%>
    <%= form.text_field :age, class: "mt-1 border-0" %>
  </div>

  <div class="mt-3">
    <%= form.submit class:"btn btn-outline-warning btn-sm" %>
  </div>

<% end %>
